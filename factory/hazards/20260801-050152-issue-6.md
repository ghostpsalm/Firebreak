# Hazards — Firebreak (fragment: issue-6, pipeline/write-safety)

## Held — found once, now guarded by a test
| hazard | path | test that holds it | commit |
|---|---|---|---|
| `toggle_reviewed` marks a row reviewed despite the underlying write failing (`Store::open` error) | src/ui.rs::toggle_reviewed | src/ui.rs::tests::toggle_reviewed_leaves_row_unmarked_and_reports_when_the_store_cannot_be_opened | ecd7c37 |
| `toggle_reviewed` marks a row reviewed despite the underlying write failing (write itself errors, e.g. SQLITE_BUSY under a held `BEGIN IMMEDIATE`) | src/ui.rs::toggle_reviewed | src/ui.rs::tests::toggle_reviewed_leaves_row_unmarked_and_reports_when_the_write_fails | ecd7c37 |
| a stale failure message in `self.status` survives a later successful toggle | src/ui.rs::toggle_reviewed | src/ui.rs::tests::toggle_reviewed_stops_reporting_the_old_failure_once_a_retry_succeeds | ecd7c37 |

## Ruled out — probed with evidence, no defect found
| hypothesis | path | evidence | established at |
|---|---|---|---|
| TOCTOU on `ri`: `self.rows` could be replaced mid-call between computing `next`/`op` and the write | src/ui.rs::toggle_reviewed | `toggle_reviewed` runs synchronously on the UI thread; `self.rows` is only replaced while draining `worker_rx` in `update()`, which the blocking write itself holds off | ecd7c37 |
| error message in `self.status` leaks sensitive data | src/ui.rs::toggle_reviewed | message contains only the DB path, and `self.status` is not rendered anywhere in the UI regardless (see #11) | ecd7c37 |
| `ReviewState::Stale` mishandled by the new match arms (special-cased only `No`/`Yes`) | src/ui.rs::toggle_reviewed | the `_` arm re-reviews with a fresh fingerprint for any non-`Yes` state, matching the existing tooltip convention at `paint.rs:2019` | ecd7c37 |
| `paint.rs:625` "Restore audit policy" has the same discard-Result idiom | src/ui/paint.rs:625 | already assigns the Result to `app.status`; not an instance of the pattern | ecd7c37 |
| `Store::set_reviewed`/`clear_reviewed` not idempotent on replay (double-toggle races) | src/store.rs | upsert/delete semantics confirmed idempotent; a failed clear correctly leaves the row `Yes(at)` intact, no partial state | ecd7c37 |

## Unprobed — no oracle available
| hypothesis | path | what would settle it |
|---|---|---|
| `ensure_secured_dir`'s real Windows ACL-failure path (vs. the Linux `create_dir_all`-on-non-dir failure used to test `Store::open` failing) | src/secure_dir.rs | a Windows host to reproduce an actual ACL/ownership failure on the DB's parent directory |
| `import_events`'s discarded `record_event` writes under a genuine `SQLITE_FULL`/IO error (not lock contention) | src/pipeline.rs:296,299 | a size-capped filesystem or disk-full repro; this is issue #7's territory, not probed as part of this run |
| `audit_control::set_auditing` failure path in `stop_auditing` (issue #12) | src/ui.rs:700 | a Windows host with a way to force the underlying policy write to fail |
